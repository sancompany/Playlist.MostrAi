<?xml version="1.0" encoding="utf-8"?>
<!--
     raiz preenche a tela física inteira (o framebuffer nativo do painel,
     sempre landscape). rotor carrega todo o conteúdo e é quem gira —
     Produto.ROTACAO_GRAUS, fixo no APK — com as margens da config como
     padding (RotacaoTela). Uma Activity só: vídeo, cartões locais,
     instalação e PIN de saída são camadas dentro de rotor.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/raiz"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <FrameLayout
        android:id="@+id/rotor"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <androidx.media3.ui.PlayerView
            android:id="@+id/player"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="false"
            app:surface_type="texture_view"
            app:resize_mode="fit"
            app:show_buffering="never" />

        <br.com.mostrai.player.ui.TelaInstitucional
            android:id="@+id/institucional"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <include layout="@layout/tela_provisionamento" />

        <include layout="@layout/tela_pin_saida" />
    </FrameLayout>
</FrameLayout>
