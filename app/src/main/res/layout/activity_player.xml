<?xml version="1.0" encoding="utf-8"?>
<!--
     raiz preenche a tela física inteira (o framebuffer nativo do painel,
     sempre landscape). rotor é quem carrega o conteúdo de verdade — seu
     tamanho e rotação são calculados em runtime (PlayerActivity) a partir
     de ConfigAparelho.rotacaoTela, para compensar um painel montado
     fisicamente de lado (comum em sinalização digital em espaço estreito).
     Sem rotação configurada (o caso comum), rotor preenche raiz normalmente.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/raiz"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <FrameLayout
        android:id="@+id/rotor"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <androidx.media3.ui.PlayerView
            android:id="@+id/player"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="false"
            app:surface_type="texture_view"
            app:resize_mode="fit"
            app:show_buffering="never" />

        <br.com.mostrai.player.ui.TelaInstitucional
            android:id="@+id/institucional"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />
    </FrameLayout>
</FrameLayout>
