<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MostraiPlayer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--
         Só para PainelActivity. Sem windowIsTranslucent, abrir o painel é
         uma troca de Activity opaca — o Android chama PlayerActivity.onStop(),
         que libera o ExoPlayer e cancela todos os temporizadores (poll,
         heartbeat, flush). Isso interrompe uma exibição paga no meio (o
         anunciante perde o comprovante daquele item) e contradiz
         docs/funcional.md, jornada do operador, item 5: "sem interromper o
         vídeo em andamento (o player continua rodando por trás)". Com a
         janela translúcida, PlayerActivity só pausa — nunca para — e o
         fundo semi-opaco (#E6000000, activity_painel.xml) finalmente cumpre
         o que o valor em si já sugeria.
    -->
    <style name="Theme.MostraiPlayer.Translucido" parent="Theme.MostraiPlayer">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
